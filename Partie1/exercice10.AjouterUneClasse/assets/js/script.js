$(function(){
  //l'ajout de .addClass() permet de changer la classe d'une balise ou id.
  $('#thirdText').addClass(hide);
  //on met .hide après l'ajout de classe sinon cela  ne sera pas pris en compte.
  $('.hide').hide();
});
